name: NASDAQ Tracker

on:
  schedule:
    # Run every 10 minutes during NYSE/NASDAQ market hours (Mon-Fri, 09:30-16:00 EST)
    # EST = UTC-5 (winter) or UTC-4 (summer/DST)
    # Using UTC 14:30-21:00 to cover regular market hours
    # Minutes: 0,10,20,30,40,50 | Hours: 14-20 UTC | Days: Mon-Fri (1-5)
    - cron: '*/10 14-20 * * 1-5'
    
    # Also run at market open (14:30 UTC) and close (21:00 UTC)
    - cron: '30 14 * * 1-5'
    - cron: '0 21 * * 1-5'
  
  # Manual trigger
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-nasdaq-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Restore database from cache
        uses: actions/cache@v4
        with:
          path: nasdaq_data.db
          key: nasdaq-db-v4-${{ github.run_id }}
          restore-keys: |
            nasdaq-db-v4-
      
      - name: Run NASDAQ Tracker
        env:
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
          DROP_THRESHOLD: ${{ vars.DROP_THRESHOLD || '5.0' }}
          HOURLY_DROP_THRESHOLD: ${{ vars.HOURLY_DROP_THRESHOLD || '3.0' }}
        run: |
          python nasdaq_tracker.py
      
      - name: Upload database artifact
        uses: actions/upload-artifact@v4
        with:
          name: nasdaq-database-${{ github.run_number }}
          path: nasdaq_data.db
          retention-days: 90
      
      - name: Commit database to repo
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          if [ -f nasdaq_data.db ]; then
            # Dosya boyutunu kontrol et (50MB'dan küçükse commit et)
            FILE_SIZE=$(stat -c%s "nasdaq_data.db" 2>/dev/null || stat -f%z "nasdaq_data.db")
            MAX_SIZE=52428800  # 50MB
            
            if [ "$FILE_SIZE" -lt "$MAX_SIZE" ]; then
              git add nasdaq_data.db
              git diff --staged --quiet || git commit -m "Update NASDAQ data - $(date -u +'%Y-%m-%d %H:%M UTC')"
              git push
            else
              echo "Database too large for git commit, using artifacts only"
            fi
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
